<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.faisalmovers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaisalMovers"
        android:windowSoftInputMode="adjustPan|adjustResize">
        <activity android:name=".RouteSelectionActivity"></activity>
        <activity android:name=".PassengerDetailsActivity"></activity>
        <activity
            android:name=".HomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SeatSelectionActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>